server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/ederfmatos/microservices-springcloud-configserver-alura
          search-paths:
            - provider
          default-label: main
          force-pull: true
